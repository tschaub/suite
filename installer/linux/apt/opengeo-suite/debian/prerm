#!/bin/sh
# prerm script for geonode
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <prerm> `remove'
#        * <old-prerm> `upgrade' <new-version>
#        * <new-prerm> `failed-upgrade' <old-version>
#        * <conflictor's-prerm> `remove' `in-favour' <package> <new-version>
#        * <deconfigured's-prerm> `deconfigure' `in-favour'
#          <package-being-installed> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    remove|upgrade|deconfigure)

	if [ -e /var/lib/tomcat6/webapps/geoexplorer ]; then
		invoke-rc.d tomcat6 stop
		rm -rf /var/lib/tomcat6/webapps/recipes /var/lib/tomcat6/webapps/dashboard
		rm -rf /var/lib/tomcat6/webapps/geoeditor /var/lib/tomcat6/webapps/geowebcache
		rm -rf /var/lib/tomcat6/webapps/geoexplorer /var/lib/tomcat6/webapps/geowebcache
		rm -rf /var/lib/tomcat6/webapps/dashboard.war
		rm -rf /var/lib/tomcat6/webapps/geoeditor.war
		rm -rf /var/lib/tomcat6/webapps/geoexplorer.war
		rm -rf /var/lib/tomcat6/webapps/geowebcache.war
		rm -rf /var/lib/tomcat6/webapps/recipes.war
		invoke-rc.d tomcat6 start
	fi
    ;;

    failed-upgrade)
    ;;

    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
