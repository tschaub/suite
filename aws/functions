# checkrc <code> <message>
function check_rc() {
  if [ "$1" != 0 ]; then
    echo "$2 failed returning $1"
    exit 1
  fi
}

# log <message>
function log() {
  echo "[`date '+%m/%d/%Y %H:%M:%S'`] $1"
}

